<UserControl x:Class="Boutique.Views.OutfitCreatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Boutique.Views"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1400">
    <Grid Margin="4,0,0,6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <views:SettingsPanelView Grid.Row="0" Margin="0,0,0,4" DataContext="{Binding}" />

        <!-- Three-panel layout: Plugins | Armors | Outfit Drafts -->
        <Grid Grid.Row="1" Margin="0,6,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="150" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="2*" MinWidth="300" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" MinWidth="280" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Plugin Selection -->
            <GroupBox Grid.Column="0" Header="Source Plugins" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <TextBox x:Name="PluginSearchBox"
                                 Text="{Binding OutfitPluginSearchText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Filter plugins by name" />
                        <TextBlock Text="🔍 Filter..."
                                   Foreground="Gray"
                                   IsHitTestVisible="False"
                                   Margin="6,0,0,0"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=PluginSearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <ListBox Grid.Row="1"
                             x:Name="PluginListBox"
                             ItemsSource="{Binding FilteredOutfitPlugins}"
                             SelectedItem="{Binding SelectedOutfitPlugin}"
                             SelectionMode="Single"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="LightGray" />

            <!-- Middle Panel: Armors from Selected Plugin -->
            <GroupBox Grid.Column="2" Header="Available Armors" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="Search:"
                                   VerticalAlignment="Center"
                                   Margin="0,0,4,0" />
                        <TextBox Grid.Column="1"
                                 Text="{Binding OutfitSearchText, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Column="2"
                                   Text="{Binding OutfitArmors.Count, StringFormat='{}{0} armors'}"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <DataGrid x:Name="OutfitArmorsGrid"
                              Grid.Row="1"
                              ItemsSource="{Binding OutfitArmorsView}"
                              SelectionChanged="OutfitArmorsGrid_SelectionChanged"
                              MouseDoubleClick="OutfitArmorsGrid_MouseDoubleClick"
                              PreviewMouseLeftButtonDown="OutfitArmorsGrid_PreviewMouseLeftButtonDown"
                              MouseMove="OutfitArmorsGrid_MouseMove"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Extended"
                              CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Width="*" IsReadOnly="True">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="DisplayName" Mode="OneWay" />
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Slots" Width="100" IsReadOnly="True">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="SlotSummary" Mode="OneWay" />
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Form ID" Width="100" IsReadOnly="True"
                                                SortMemberPath="FormIdSortable">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="FormIdDisplay" Mode="OneWay" />
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="2"
                            Content="Create New Outfit from Selected"
                            Command="{Binding CreateOutfitCommand}"
                            HorizontalAlignment="Right"
                            Margin="0,4,0,0"
                            Padding="12,6" />
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="3" Width="4" HorizontalAlignment="Stretch" Background="LightGray" />

            <!-- Right Panel: Outfit Drafts (unchanged) -->
            <GroupBox Grid.Column="4" Header="Outfit Queue" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border x:Name="NewOutfitDropZone"
                            Grid.Row="1"
                            Margin="0,0,0,6"
                            Padding="8"
                            Background="#08007ACC"
                            BorderBrush="#FF7FAFF6"
                            BorderThickness="1"
                            CornerRadius="4"
                            AllowDrop="True"
                            DragEnter="NewOutfitDropZone_OnDragEnter"
                            DragLeave="NewOutfitDropZone_OnDragLeave"
                            DragOver="NewOutfitDropZone_OnDragOver"
                            Drop="NewOutfitDropZone_OnDrop">
                        <TextBlock Text="Drop armors here to create a new outfit (double-click an armor to quick add)."
                                   Foreground="Gray"
                                   FontStyle="Italic"
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>

                    <Border Grid.Row="0"
                            Margin="0,0,0,6"
                            Padding="8"
                            Background="#10FFFFFF"
                            BorderBrush="#FF7FAFF6"
                            BorderThickness="1"
                            CornerRadius="4">
                        <Border.Visibility>
                            <Binding Path="HasExistingPluginOutfits">
                                <Binding.Converter>
                                    <BooleanToVisibilityConverter />
                                </Binding.Converter>
                            </Binding>
                        </Border.Visibility>
                        <StackPanel>
                            <TextBlock Text="{Binding ExistingOutfits.Count, StringFormat='This plugin already defines {0} outfit(s).'}"
                                       FontWeight="Bold"
                                       Margin="0,0,0,6" />
                            <ScrollViewer MaxHeight="120"
                                          VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding ExistingOutfits}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Summary}"
                                                       Foreground="Gray"
                                                       Margin="0,0,0,2" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <Button Content="Copy Existing Outfits"
                                    Command="{Binding CopyExistingOutfitsCommand}"
                                    HorizontalAlignment="Right"
                                    Margin="0,6,0,0"
                                    Padding="10,4" />
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="2">
                        <TextBlock Text="No outfits queued. Select armors and click Create Outfit."
                                   Foreground="Gray" FontStyle="Italic"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasOutfitDrafts}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding OutfitDrafts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,6"
                                                Padding="6"
                                                BorderBrush="#FFD9D9D9"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                AllowDrop="True"
                                                DragEnter="OutfitDraftBorder_OnDragEnter"
                                                DragLeave="OutfitDraftBorder_OnDragLeave"
                                                DragOver="OutfitDraftBorder_OnDragOver"
                                                Drop="OutfitDraftBorder_OnDrop">
                                            <StackPanel>
                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Vertical">
                                                        <TextBox Width="180"
                                                                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 PreviewTextInput="OutfitNameTextBox_OnPreviewTextInput"
                                                                 DataObject.Pasting="OutfitNameTextBox_OnPasting"
                                                                 MaxLength="40"
                                                                 HorizontalAlignment="Left" />
                                                        <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="Gray">
                                                            <Run Text="{Binding EditorId, Mode=OneWay}" />
                                                            <Run Text=" • " />
                                                            <Run Text="{Binding FormIdDisplay, Mode=OneWay}" />
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                                                                VerticalAlignment="Top">
                                                        <Button Content="👁"
                                                                Command="{Binding PreviewCommand}"
                                                                Padding="6,4"
                                                                Margin="0,0,4,0"
                                                                ToolTip="Preview outfit" />
                                                        <Button Content="✕"
                                                                Command="{Binding RemoveSelfCommand}"
                                                                Padding="6,4"
                                                                ToolTip="Remove outfit" />
                                                    </StackPanel>
                                                </Grid>
                                                <ItemsControl ItemsSource="{Binding Pieces}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,2,0,2">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0"
                                                                           Text="{Binding SummaryLine}"
                                                                           VerticalAlignment="Center"
                                                                           TextWrapping="NoWrap"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           FontSize="12" />
                                                                <Button Grid.Column="1" Content="✕"
                                                                        Command="{Binding DataContext.RemovePieceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                        CommandParameter="{Binding}"
                                                                        Padding="4,2"
                                                                        Margin="4,0,0,0"
                                                                        FontSize="10"
                                                                        ToolTip="Remove piece" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <TextBlock Text="No pieces selected." Foreground="Gray"
                                                           FontStyle="Italic" Margin="0,2,0,0" FontSize="12">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding HasPieces}"
                                                                             Value="False">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="{Binding OutfitDrafts.Count, StringFormat='{}{0} outfit(s) queued'}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Save Outfits" Command="{Binding SaveOutfitsCommand}" Padding="16,8" FontWeight="Bold">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasOutfitDrafts}" Value="False">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsCreatingOutfits}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</UserControl>
