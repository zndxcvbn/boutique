<UserControl x:Class="Boutique.Views.DistributionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Boutique.Views"
             mc:Ignorable="d"
             x:Name="Root"
             d:DesignHeight="800"
             d:DesignWidth="1200">
    <Grid Margin="4,0,0,6">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TabControl with Create, NPCs, and Outfits tabs -->
        <TabControl Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex}">

            <!-- CREATE TAB -->
            <TabItem Header="Create">
                <views:DistributionCreateTabView DataContext="{Binding EditTab}" />
            </TabItem>

            <!-- FILES TAB - Disabled, may remove later
            <TabItem Header="Files">
                <views:DistributionFilesTabView DataContext="{Binding FilesTab}" />
            </TabItem>
            -->

            <!-- NPCs TAB -->
            <TabItem Header="NPCs">
                <views:DistributionNpcsTabView DataContext="{Binding NpcsTab}" />
            </TabItem>

            <!-- OUTFITS TAB -->
            <TabItem Header="Outfits">
                <views:DistributionOutfitsTabView DataContext="{Binding OutfitsTab}" />
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="1"
                   Margin="0,6,0,0"
                   Text="{Binding StatusMessage}"
                   FontStyle="Italic" />
    </Grid>
</UserControl>
